name: "200 · Build Container Images"

# Runs on GitHub-hosted runners (ubuntu-latest). Nothing here needs a private
# network or cloud credentials: images build with buildx (GitHub Actions cache),
# push to Docker Hub with the repository secret, and the manifest commit and
# appliance dispatch use GitHub tokens. The self-hosted runner the original
# repository used is retired; a fresh clone of this repository needs no runner.

on:
  push:
    branches:
      - main
    paths:
      - Dockerfile
      - apps/cna-api/**
      - apps/cna-worker/**
      - apps/cna-web/**
      - cna/**
      - scripts/drawio-headless.sh
      - pyproject.toml
      - .github/workflows/200-build-images.yml
  workflow_dispatch:

# A newer push to main makes an in-flight build's images stale before they
# even finish (e.g. rapid Dependabot merges) — cancel the superseded run
# instead of letting it finish and burn runner time behind it.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  # CLI, API, and Worker share an identical build shape (single-stage, root
  # context, no extra validation) so they run as one matrix. Web is separate
  # below because it adds npm audit, a second (migrator) target, and a smoke test.
  build-images:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      DOCKERHUB_NAMESPACE: ${{ vars.DOCKERHUB_NAMESPACE }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: CLI
            file: Dockerfile
            raw_tag: latest
            sha_prefix: ""
          - name: API
            file: apps/cna-api/Dockerfile
            raw_tag: api-latest
            sha_prefix: api-sha-
          - name: Worker
            file: apps/cna-worker/Dockerfile
            raw_tag: worker-latest
            sha_prefix: worker-sha-
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@594f3bf4285d9ea8dc53c9a0c9c4092420091003  # v4.4.0

      - name: Validate Docker Hub namespace
        run: |
          if [ -z "$DOCKERHUB_NAMESPACE" ]; then
            echo "::error::DOCKERHUB_NAMESPACE variable is required."
            exit 1
          fi

      - name: Log in to Docker Hub
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f  # v4.6.0
        with:
          registry: docker.io
          username: ${{ vars.DOCKERHUB_NAMESPACE }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract metadata for ${{ matrix.name }}
        id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302  # v6.2.0
        with:
          images: docker.io/${{ env.DOCKERHUB_NAMESPACE }}/cna
          tags: |
            type=raw,value=${{ matrix.raw_tag }}
            type=sha,prefix=${{ matrix.sha_prefix }}

      - name: Build and push ${{ matrix.name }} image
        id: build
        uses: docker/build-push-action@c3c9e263c25d99ce0380d002d59b67737d91b0dc  # v7.4.0
        with:
          context: .
          file: ${{ matrix.file }}
          build-args: |
            CNA_BUILD_SHA=${{ github.sha }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          provenance: true
          sbom: true
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # Scout by digest, not by the mutable `:${{ matrix.raw_tag }}` tag: buildx
      # --push never loads the image into the runner's local Docker daemon, and
      # docker/scout-action can silently fall back to resolving a floating tag
      # against a stale image the daemon already has cached locally (e.g. from a
      # much older build), scanning the wrong content entirely.
      - name: Docker Scout — fail on fixable critical/high CVEs
        uses: docker/scout-action@7c6b6c3f7844478ace1ffd4e7aef649053d1f87d  # v1.24.0
        with:
          command: cves
          image: docker.io/${{ env.DOCKERHUB_NAMESPACE }}/cna@${{ steps.build.outputs.digest }}
          only-severities: critical,high
          only-fixed: true
          exit-code: true

  build-web:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      DOCKERHUB_NAMESPACE: ${{ vars.DOCKERHUB_NAMESPACE }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@594f3bf4285d9ea8dc53c9a0c9c4092420091003  # v4.4.0

      - name: Log in to Docker Hub
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f  # v4.6.0
        with:
          registry: docker.io
          username: ${{ vars.DOCKERHUB_NAMESPACE }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Validate Docker Hub namespace
        run: |
          if [ -z "$DOCKERHUB_NAMESPACE" ]; then
            echo "::error::DOCKERHUB_NAMESPACE variable is required."
            exit 1
          fi

      - name: Extract metadata for Web
        id: meta_web
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302  # v6.2.0
        with:
          images: docker.io/${{ env.DOCKERHUB_NAMESPACE }}/cna
          tags: |
            type=raw,value=web-latest
            type=sha,prefix=web-sha-

      - name: Extract metadata for Migrator
        id: meta_migrator
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302  # v6.2.0
        with:
          images: docker.io/${{ env.DOCKERHUB_NAMESPACE }}/cna
          tags: |
            type=raw,value=migrator-latest
            type=sha,prefix=migrator-sha-

      - name: npm audit — check for known vulnerabilities
        # Fail on critical/high vulnerabilities before building the image.
        # Audit-level=high: moderate issues are warned, not failing (acceptable for beta deps).
        # Run from apps/cna-web since package-lock.json is scoped to that directory.
        run: |
          cd apps/cna-web
          npm audit --audit-level=high --omit=dev 2>&1 || {
            echo "::warning::npm audit found issues — review above output"
            echo "::warning::Run 'npm audit fix' locally if issues are exploitable in production context"
            # Fail only on critical issues (exit code 16 = critical)
            npm audit --audit-level=critical --omit=dev
          }

      - name: Build and push Web image
        id: build_web
        uses: docker/build-push-action@c3c9e263c25d99ce0380d002d59b67737d91b0dc  # v7.4.0
        with:
          context: apps/cna-web
          file: apps/cna-web/Dockerfile
          target: runner
          build-args: |
            CNA_BUILD_SHA=${{ github.sha }}
          push: true
          tags: ${{ steps.meta_web.outputs.tags }}
          labels: ${{ steps.meta_web.outputs.labels }}
          provenance: true
          sbom: true
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # Scout by digest, not by the mutable `:web-latest` tag — see comment on
      # the CLI/API/Worker Scout step above for why: buildx --push never loads
      # the image into the runner's local daemon, so a floating-tag reference
      # can resolve against a stale locally-cached image instead of the one
      # just pushed.
      - name: Docker Scout — fail on fixable critical/high CVEs (Web)
        uses: docker/scout-action@7c6b6c3f7844478ace1ffd4e7aef649053d1f87d  # v1.24.0
        with:
          command: cves
          image: docker.io/${{ env.DOCKERHUB_NAMESPACE }}/cna@${{ steps.build_web.outputs.digest }}
          only-severities: critical,high
          only-fixed: true
          exit-code: true

      - name: Build and push Migrator image
        id: build_migrator
        uses: docker/build-push-action@c3c9e263c25d99ce0380d002d59b67737d91b0dc  # v7.4.0
        with:
          context: apps/cna-web
          file: apps/cna-web/Dockerfile
          target: migrator
          build-args: |
            CNA_BUILD_SHA=${{ github.sha }}
          push: true
          tags: ${{ steps.meta_migrator.outputs.tags }}
          labels: ${{ steps.meta_migrator.outputs.labels }}
          provenance: true
          sbom: true
          cache-from: type=gha

      - name: Docker Scout — fail on fixable critical/high CVEs (Migrator)
        uses: docker/scout-action@7c6b6c3f7844478ace1ffd4e7aef649053d1f87d  # v1.24.0
        with:
          command: cves
          image: docker.io/${{ env.DOCKERHUB_NAMESPACE }}/cna@${{ steps.build_migrator.outputs.digest }}
          only-severities: critical,high
          only-fixed: true
          exit-code: true

      - name: Smoke test — /api/health returns 200
        env:
          WEB_DIGEST: ${{ steps.build_web.outputs.digest }}
        run: |
          # Run by digest, not the mutable `:web-latest` tag: `docker run` also
          # prefers a stale locally-cached image over pulling the one just
          # pushed (same issue as the Docker Scout steps above), which would
          # smoke-test the wrong image entirely.
          docker run -d --name cna-web-smoke \
            -e DATABASE_URL="postgresql://test:test@localhost:5432/test?sslmode=disable" \
            -e NEXTAUTH_URL="http://localhost:3000" \
            -e NEXTAUTH_SECRET="ci-smoke-secret-not-real" \
            -e AZURE_AD_CLIENT_ID="00000000-0000-0000-0000-000000000000" \
            -e AZURE_AD_CLIENT_SECRET="ci-placeholder" \
            -e AZURE_AD_TENANT_ID="00000000-0000-0000-0000-000000000000" \
            -p 3000:3000 \
            "docker.io/${DOCKERHUB_NAMESPACE}/cna@${WEB_DIGEST}"
          sleep 15
          # Always dump logs so we can diagnose crashes even on failure
          echo "--- container status ---"
          docker ps -a --filter name=cna-web-smoke --format "table {{.Status}}\t{{.Ports}}"
          echo "--- container logs ---"
          docker logs cna-web-smoke || true
          echo "--- end logs ---"
          # Health check returns 503 without a real DB — verify the process started
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/api/health || true)
          docker rm -f cna-web-smoke
          # 200 = healthy DB, 503 = process started but DB unreachable (expected in CI)
          if [ "$STATUS" != "200" ] && [ "$STATUS" != "503" ]; then
            echo "Unexpected status: $STATUS — container may have failed to start"
            exit 1
          fi
          echo "Smoke test passed (status=$STATUS)"

  publish-build-manifest:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: [build-images, build-web]
    env:
      DOCKERHUB_NAMESPACE: ${{ vars.DOCKERHUB_NAMESPACE }}
    # The manifest is handed to notify-appliances as a job output so that job
    # does not have to re-checkout main and race the push below.
    outputs:
      manifest: ${{ steps.manifest.outputs.json }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          # Need write access to push the manifest commit back to main
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Write build manifest
        id: manifest
        run: |
          if [ -z "$DOCKERHUB_NAMESPACE" ]; then
            echo "::error::DOCKERHUB_NAMESPACE variable is required."
            exit 1
          fi
          SHA_TAG="sha-$(echo '${{ github.sha }}' | cut -c1-7)"
          mkdir -p .deployment-catalog
          cat > .deployment-catalog/latest-build.json <<EOF
          {
            "sha_tag": "${SHA_TAG}",
            "commit": "${{ github.sha }}",
            "built_at": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "run_id": "${{ github.run_id }}",
            "images": {
              "cli":      "docker.io/${DOCKERHUB_NAMESPACE}/cna:${SHA_TAG}",
              "api":      "docker.io/${DOCKERHUB_NAMESPACE}/cna:api-${SHA_TAG}",
              "worker":   "docker.io/${DOCKERHUB_NAMESPACE}/cna:worker-${SHA_TAG}",
              "web":      "docker.io/${DOCKERHUB_NAMESPACE}/cna:web-${SHA_TAG}",
              "migrator": "docker.io/${DOCKERHUB_NAMESPACE}/cna:migrator-${SHA_TAG}"
            }
          }
          EOF
          echo "Manifest written:"
          cat .deployment-catalog/latest-build.json
          echo "json=$(jq -c . .deployment-catalog/latest-build.json)" >> "$GITHUB_OUTPUT"

      - name: Commit and push manifest
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .deployment-catalog/latest-build.json
          git diff --cached --quiet && echo "No changes to commit" && exit 0
          git commit -m "chore(build): update latest-build manifest to sha-$(echo '${{ github.sha }}' | cut -c1-7) [skip ci]"
          # Pull rebase in case main moved while builds were running (common in parallel jobs).
          # -X theirs: during conflicts, prefer our new manifest over whatever is on remote.
          git pull --rebase --autostash -X theirs origin main
          git push

  # Tell each appliance repository that a new image set exists so it can
  # auto-update dev and open the prod update request (its 230-image-update
  # workflow). This is the push-based fast path; the appliances also poll the
  # committed manifest, so a failed dispatch is a warning, never a failed build.
  #
  # APPLIANCE_REPOS is a comma-separated list of repository names under this
  # owner (e.g. "Work-Cloud_Network_Azure_Appliance,Work-Cloud_Network_AWS_Appliance").
  # The GitHub App must be installed on each of them; the token is scoped to
  # exactly those repositories.
  notify-appliances:
    runs-on: ubuntu-latest
    needs: publish-build-manifest
    if: vars.APPLIANCE_REPOS != ''
    steps:
      - name: Generate GitHub App token (appliance repositories only)
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1  # v3.2.0
        with:
          client-id: ${{ secrets.GH_APP_ID }}
          private-key: ${{ secrets.GH_APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}
          repositories: ${{ vars.APPLIANCE_REPOS }}

      - name: Send cna-image-published to each appliance
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          OWNER: ${{ github.repository_owner }}
          APPLIANCE_REPOS: ${{ vars.APPLIANCE_REPOS }}
          MANIFEST_JSON: ${{ needs.publish-build-manifest.outputs.manifest }}
        run: |
          set -u
          if [ -z "$MANIFEST_JSON" ]; then
            echo "::warning::No build manifest output; appliances will pick the images up on their next poll."
            exit 0
          fi
          PAYLOAD=$(jq -cn --argjson m "$MANIFEST_JSON" '{event_type: "cna-image-published", client_payload: $m}')
          IFS=',' read -ra REPOS <<< "$APPLIANCE_REPOS"
          for RAW in "${REPOS[@]}"; do
            REPO=$(echo "$RAW" | xargs)
            [ -n "$REPO" ] || continue
            STATUS=$(curl -sS -o /tmp/dispatch.out -w '%{http_code}' -X POST \
              -H "Authorization: Bearer $GH_TOKEN" \
              -H "Accept: application/vnd.github+json" \
              -H "X-GitHub-Api-Version: 2022-11-28" \
              "https://api.github.com/repos/${OWNER}/${REPO}/dispatches" \
              -d "$PAYLOAD" || echo "000")
            if [ "$STATUS" = "204" ]; then
              echo "Notified ${OWNER}/${REPO} (cna-image-published $(jq -r .sha_tag <<< "$MANIFEST_JSON"))"
            else
              echo "::warning::Could not notify ${OWNER}/${REPO} (HTTP ${STATUS}); it will pick the images up on its next scheduled poll."
              cat /tmp/dispatch.out 2>/dev/null || true
            fi
          done
